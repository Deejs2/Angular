import { NepaliNumberPipe } from './nepali-number.pipe';

describe('NepaliNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new NepaliNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
